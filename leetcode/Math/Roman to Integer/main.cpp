class Solution {
public:
    int romanToInt(string s) {
        string symbol[7] = { "I", "IV", "V", "IX", "X", "XL", "L", "XC", "C", "CD", "D", "CM", "M" };
        int val[7] = { 1, 4, 5, 9, 10, 40, 50, 90, 100, 400, 500, 900 1000 };

        for(size_type i = 0; i < s.size(); ++i) {
            do_things_with(s[i]);
        }
        for ( int i=0; i<s.size(); i++ ) {
            if ( s[i] == 'I' ) {

            }
            else if ()
        }

    }
};