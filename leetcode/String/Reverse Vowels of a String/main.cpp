class Solution {
public:
    string reverseVowels(string s) {
        char *ptr, *qtr;
        int len = s.size() - 1;
        string vowel = "aeiouAEIOU"
        ptr = s;
        qtr = s[len];

        while ( ptr < qtr ) {
            
        }
    }
};